[title]
content_id = "VSH"
short_name = "vsh"
display_name = "PS3 System Software (vsh)"
eboot_candidates = ["vsh.self"]
year = 2006
developer = "Sony Interactive Entertainment"
engine = "PAF"
distribution = "firmware-exec"

[source]
kind = "firmware-exec"
path = "dev_flash/vsh/module"

[checkpoint]
kind = "process-exit"

[rsx]
mirror = false
consume = false

# The one cell this title is gated and rendered at; see
# docs/architecture/title_harness.md.
[[bench.matrix]]
fw = "4.93"
reference = true
