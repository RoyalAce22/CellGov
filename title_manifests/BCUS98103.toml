[title]
content_id = "BCUS98103"
short_name = "uncharted"
display_name = "Uncharted: Drake's Fortune"
eboot_candidates = ["EBOOT.BIN", "EBOOT.elf"]
year = 2007
developer = "Naughty Dog"
engine = "Naughty Dog proprietary"
distribution = "disc-iso"
# PS3_SYSTEM_VER from the title's own PARAM.SFO (01.9400), held against
# the installed content by the `installed-title-tests` suite. Derives the one cell
# the headline row is measured at: fw 1.94 x base.
system_ver = "1.94"

[source]
kind = "disc"

[checkpoint]
kind = "first-rsx-write"

# Drift study: the same title on the newest firmware. Recorded evidence
# about how much of a divergence is the firmware's rather than the
# title's.
[[bench.matrix]]
fw = "1.94"
game_ver = "base"
pending = "the resident title image or firmware set crosses the fixed TLS reservation, so boot refuses before TLS initialization"

[[bench.matrix]]
fw = "4.93"
game_ver = "base"
pending = "the resident title image or firmware set crosses the fixed TLS reservation, so boot refuses before TLS initialization"
