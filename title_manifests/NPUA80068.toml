[title]
content_id = "NPUA80068"
short_name = "sshd"
display_name = "Super Stardust HD"
eboot_candidates = ["EBOOT.BIN", "EBOOT.elf"]
year = 2007
developer = "Housemarque"
engine = "Housemarque proprietary"
distribution = "psn-hdd"
rap_filename = "UP9000-NPUA80068_00-STARDUSTFULL0001.rap"
# This title does not reach its checkpoint -- the cap is what ends the
# run, so its recorded MaxSteps outcome and step count only reproduce
# at this exact value.
bench_max_steps = 100_000_000

[checkpoint]
kind = "first-rsx-write"

# The firmware this title's own PARAM.SFO asks for
# (PS3_SYSTEM_VER = 03.7000). Kept declared so the anchors committed for
# it stay evidence about a cell somebody chose.
[[bench.matrix]]
fw = "3.70"
game_ver = "base"

# The cell this title is gated and rendered at. See
# docs/architecture/title_harness.md.
[[bench.matrix]]
fw = "4.93"
game_ver = "base"
reference = true
